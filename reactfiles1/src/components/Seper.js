import React from 'react'


const Seper = (props) =>{
    
    return(
        <div>
            <h1>{props.person.id} name is {props.person.name}</h1>
        </div>
    )
    
}

export default Seper